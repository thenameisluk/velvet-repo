#!/bin/bash
cd "$(dirname "$0")"

VERSION=0.4

git clone --depth 1 --branch $VERSION https://github.com/thenameisluk/velvettools/

cd velvettools
./build_deb
cd ..
if [ "$1" == "" ]; then
    mv velvettools/*.deb . -v
else
    mv velvettools/*.deb "$1" -v
fi

rm -rf velvettools