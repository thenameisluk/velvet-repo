#!/bin/bash
#run inside scirpt dir
cd "$(dirname "$0")"

rm -rf ../output #clean up previous build

mkdir ../output

#build all packages
for buildme in ./packages/*/buildme; do

    chmod +x "./${buildme}"
    "./${buildme}" "$(pwd)/../output" > /dev/null 2>/dev/null

done



